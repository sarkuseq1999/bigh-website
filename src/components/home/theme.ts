// Golden Hour design tokens — mirrors the `.gh` scope in globals.css.
// Import these when a component needs the values in JS (canvas, GSAP, three.js).
export const GH = {
  bg: "#04080f",
  fg: "#e6f0f7",
  muted: "#b9cbd9",
  faint: "#6f8aa0",
  ember: "#e9946a",
  line: "rgba(230, 240, 247, 0.12)",
} as const;

# Public demo — September 22, 2026

Mo explicitly requested a public demo after colleagues could not open the tokenized preview links.

**Share this stable address: https://bigh-website-demo.vercel.app/**

The alias currently points to `bigh-website-lktxa1961-sarkuseq1999s-projects.vercel.app`, published September 24, 2026 by Claude Code. The customer stories show the chosen [Still life design](customer-stories-v2.md). Directly after them, "Make sense of the science." shows the chosen [Glass cell](science-section.md) with topic tabs and a picture for each topic; no review bar remains. The accepted sample testimonials remain visibly labeled as fictional. `bigh-website-pxg4848tn-…` compared round 2's three looks. `bigh-website-7383r0ugs-…` showed round 1 (Kitchen science, Myth or fact, Chat). Earlier that day, `bigh-website-ipzwvp89c-…` compared the three story designs (Still life, Daylight, Showroom), and `bigh-website-722zsslah-…` showed the ink-drawn Portraits. Everything else is unchanged from the September 23 deployment `dpl_xVL3YupMpTiwmVN4Sw4XF7ZtL7Nr`, which included the earlier [Daybook, Story Wall, and Spotlight choices](customer-stories-comparison.md). It also includes the [approved product introduction and refined story panels](products-heading-final.md): “Explore our products. Find your starting point.”, a new coastal-walk image for NuriCell, and two illustrated highlights for every product. The headline comparison controls are removed. The oversized flagship experiment remains removed; the quiet “BiGH’s flagship formula” caption remains below NuriCell’s name. The repeated collection label and badge above the bottle remain removed. The glowing mitochondria and drifting background remain. At Mo’s earlier request, the paragraph starting “His 2002 study examined…” remains removed from Dr. Liu’s profile and all five translation catalogs. His other biography text and source links remain.

## Access configuration

- Project: `bigh-website`, ID `prj_A2Uwj2493b7kSTT4o2cyDz4EuKfr`.
- Team: `sarkuseq1999s-projects`, ID `team_oVYrgPRQtRjv5OAtT6T7glrJ`.
- Only `bigh-website-demo.vercel.app` has a public domain exception (`alias-protection-override`). No query string, cookie, or Vercel account is required to open it.
- Project-wide Vercel Authentication remains `all_except_custom_domains`. The attempted project-wide removal was rejected by automatic approval review because it would expose every applicable older deployment; that change did not run. The narrower demo-domain exception was approved and applied.
- The original production domain was not reassigned or promoted. Old generated preview links retain their existing access rules; send the short public demo address instead.
- Existing no-index metadata remains as implemented. This is public demo access, not a change to launch content.

## Publishing future changes

Build and verify the requested changes, then publish a preview. After that deployment is ready, update the existing alias:

```powershell
vercel alias set <new-deployment-hostname> bigh-website-demo.vercel.app --scope sarkuseq1999s-projects
```

Keep the same public address. Verify that it loads in a fresh unauthenticated browser without any query parameters and shows the newest change. Do not remove project-wide authentication or recreate tokenized share links for normal demos.

## Verification

Research list, September 24 (late): 36 checked sources in three filters with a show-more control, no rats mentioned. After aliasing, the demo returned HTTP 200; the research list's 4 checks and the science section's 21 checks passed live in English and Korean at 1440 and 390 wide.

Quiet link, September 24 (late): a "Discover NuriCell" text link in "Our purpose" and the two older blocks' labels renumbered 07 and 08. Live check after aliasing: the labels read 07 and 08 and the link opens the NuriCell product.

Page ending, September 24 (late): the closing block is no longer rendered, so the page ends on the research list and "Our purpose" before the footer, at Mo's request. After aliasing, the demo returned HTTP 200, the science section's 21 checks passed live in English and Korean, and a live check confirmed no closing block, no footer headline, and "about" as the last block.

Closing block, new picture, September 24 (late): the Space look now shows a constellation of cells instead of the hero's cell. After aliasing, the demo and the new picture returned HTTP 200, and the closing block's 12 checks passed live in English and Korean at 1440 and 390 wide.

Closing block, September 24 (evening): three looks for "Start with your cells." (`?closing=1|2|3`) and the footer's old headline removed. After aliasing, the demo returned HTTP 200; the closing block's 12 checks and the science section's 21 checks passed in a fresh browser in English and Korean at 1440 and 390 wide.

Glass cell, gradual aging and the closing button, September 24 (evening): after aliasing, the plain demo URL returned HTTP 200, 21 of 21 checks passed in a fresh browser in English and Korean at 1440 and 390 wide, and the closing button measured 77 px below the last topic's words at 1440 x 900.

Glass cell, third refinement, September 24: the free-radical scene redrawn inside the glass, antioxidant droplets and the aged render. After aliasing, the plain demo URL returned HTTP 200, and 21 of 21 checks passed in a fresh browser in English and Korean at 1440 and 390 wide.

Glass cell, second refinement, September 24: the free-radical scene was reworked and the age slider added. After aliasing, the plain demo URL returned HTTP 200, and 21 of 21 checks passed in a fresh browser in English and Korean at 1440 and 390 wide. They cover the topics on scroll, the slider (demo, keyboard, drag), the scene's three steps, tabs, explainers, reduced motion, and no sideways scroll or errors.

Glass cell refinement, September 24: the hosted build reported ready and the alias returned HTTP 200 with no redirect. In a fresh browser, 13 of 13 checks passed on the public URL in English and Korean at 1440 and 390 wide. They cover WebGL readiness, topics 1-2-3 with matching tabs and pictures on scroll, the age dial reaching 80, tab jumps, each topic's explainer, reduced motion, and no sideways scroll or errors. General project authentication was not changed.

Science-section round 2, September 24: the hosted build reported ready and the alias returned HTTP 200 with no redirect. In a fresh browser, the same 29 checks that passed locally passed on the public URL in English and Korean at 1440 and 390 wide. They cover WebGL readiness and chapter changes on scroll for Glass cell, the rail jump, the camera moving and labels per chapter for Kitchen table, the bulb and apple changing and the close-up on hover for Picture words, every explainer link, reduced motion, and no sideways scroll or errors. General project authentication was not changed.

Science-section review, September 24: the hosted build reported ready and the alias returned HTTP 200 with no redirect. In a fresh browser, the same 30 checks that passed locally passed on the public URL in English and Korean at 1440 and 390 wide. They cover the scroll-linked pictures, the Myth-or-fact game, the chat thread with the explainer dialog, reduced motion, and no sideways scroll or errors. The stories section showed Still life with no review bar. General project authentication was not changed.

Chosen Portraits design with ink drawings, September 24: the local production build compiled, passed TypeScript, and generated 10 pages. After the alias update, the plain demo URL returned HTTP 200 with no redirect. In a fresh browser, 21 of 21 checks passed on the public URL. They cover all five languages at 1440 and 390 wide (no sideways scroll, ink portrait and face buttons loaded, no review bar, no page errors; on phones all three face buttons in one row above the drawing), plus story switching by click and arrow key, both labels visible, and the product dialog opening and closing with Escape. General project authentication was not changed.

Three new customer-story designs, September 24: the local production build compiled, passed TypeScript, and generated 10 pages; the preview reported ready. After the alias update, a fresh unauthenticated browser opened the plain demo URL with HTTP 200 and no login redirect, and showed the Portraits / Moments / Scroll story controls. The same 19 interaction checks that passed locally passed on the public URL: story switching by click and arrow keys, video pause/play, reel scrolling, product dialogs with Escape, and reduced motion. The portrait image loaded on scroll, and on-screen videos played while the off-screen one waited. No page errors. General project authentication was not changed.

Three customer-story designs, September 23: final local and hosted builds compiled, passed TypeScript, generated 10 pages, and reported READY. After aliasing, a fresh unauthenticated browser opened the plain demo URL with HTTP 200 and no login redirect. All three design controls switched correctly; Daybook and Spotlight displayed one selected story, while Story Wall displayed all three. Sample labels remained visible and all images decoded successfully. Story navigation and the Green Bee Propolis dialog worked. At 390px there was no horizontal page overflow or clipped comparison button. Zero page or console errors were recorded. Hosted screenshots are saved as `reference/customer-stories/compare-public-*`; the gallery and phone screenshots were visually inspected. General authentication was not changed; the demo's public-access exception persisted.

Three customer-story designs, September 23: the final local and hosted builds compiled, passed TypeScript, generated 10 pages, and reported READY. After aliasing, a new unauthenticated browser opened the plain demo URL with HTTP 200 and no login redirect. All three design controls switched the layout correctly. Daybook and Spotlight showed one selected story; Story Wall showed all three. Every sample retained its label and all images decoded successfully. Story navigation and the Green Bee Propolis product dialog worked. At 390px, no page overflow or clipped comparison buttons occurred. Zero page or console errors were recorded. Screenshots of all three hosted desktop designs and the phone layout are saved under `reference/customer-stories/compare-public-*`; the hosted gallery and phone screenshots were visually inspected. Existing project-wide authentication remained untouched and the public demo exception persisted.

Customer stories, September 23: local and hosted builds compiled, passed TypeScript, and generated 10 pages; the preview reported READY. After updating the alias, a fresh unauthenticated browser opened the plain public URL with HTTP 200 and no login redirect. The new section appeared immediately after Products, with its heading and visible fictional-sample notice. Next controls displayed the second and third stories; the third opened the Green Bee Propolis dialog and Escape closed it. Product images loaded. At 390px there was no horizontal page overflow. Zero page or console errors were recorded. Hosted desktop and phone screenshots (`reference/customer-stories/public-desktop.png` and `public-phone.png`) were visually inspected. General protection settings were not changed; public access on the demo alias persisted.

Latest approved introduction, September 23: local and hosted builds compiled, passed TypeScript, and generated 10 pages; the new preview reported READY. After updating the alias, a fresh unauthenticated browser opened the plain public URL with HTTP 200 and no login redirect. The exact approved headline and supporting sentence appeared, the new coastal image loaded, all five products displayed two highlights, and no comparison buttons remained. The phone layout had no horizontal overflow; the NuriCell dialog opened and closed. Zero page or console errors were recorded. Hosted desktop and phone screenshots (`products-final-public-desktop.png` and `products-final-public-phone.png` in `reference/products-section/`) were visually inspected. The public domain exception persisted; general project authentication was not changed.

Latest story imagery update, September 23: the final hosted build compiled, passed TypeScript, generated all 10 pages, and reported READY. After the alias update, a fresh unauthenticated browser opened the plain public URL (no query parameters) with HTTP 200 and no login redirect. All three new headlines appeared, all five corresponding story images loaded, and the NuriCell dialog opened and closed. The phone viewport has matching document client and scroll widths (375px), so there is no sideways scroll. NuriCell uses its restored 294px phone image height. Zero page or console errors occurred. Hosted desktop and phone screenshots were saved as `reference/products-section/story-public-desktop.png` and `story-public-phone.png` and visually inspected. The existing public exception persisted; no general protection settings changed.

Latest elegant variations, September 23: local and hosted production builds compiled, passed TypeScript, generated 10 pages, and reported READY. After updating the alias, a fresh unauthenticated browser opened the plain public URL with HTTP 200. All three controls switched the heading and flagship presentation together, with NuriCell explicitly identified in each. All five images loaded. At 390px the controls are a compact 52px row, the page has no horizontal overflow, and NuriCell's dialog opens. No page or console errors occurred. Hosted desktop and phone screenshots were captured and visually inspected. The public exception persisted and no project-wide access setting was changed.

Latest heading update, September 23: local and hosted builds compiled, passed TypeScript, generated 10 pages, and reported READY. After the alias update, the plain public URL returned HTTP 200 in a fresh anonymous browser. All three heading controls displayed the expected headline with exactly one selected option. The old visible label, heading, and bottle badge are absent; all five bottle images load on a white background. The phone view has no horizontal overflow, and NuriCell's preview dialog opens. Zero page or console errors were recorded. Hosted screenshots: `reference/products-section/heading-public-desktop.png` and `heading-public-phone.png`. No project protection settings were changed.

September 23 lineup update: local and hosted production builds passed and generated all 10 pages; deployment reported READY. After updating the alias, opened the plain public URL in a new anonymous browser context with no query parameters: HTTP 200, no login redirect. The refined lineup is active with NuriCell initially selected. All five bottles reveal their corresponding stories; the NuriCell Discover button opens its existing dialog. At 390px, selecting OPC brings its story into view with no horizontal overflow. All bottle images loaded, and the fresh browser recorded zero page or console errors. Screenshots: `reference/products-section/lineup-public-desktop.png` and `lineup-public-phone.png`. The alias's public exception persisted; no project-wide protection setting was changed.

Latest description update: opened Dr. Liu’s profile in all five languages locally and confirmed the requested paragraph is gone. Changed-file lint and formatting passed; the deployment compiled, passed TypeScript, generated all 10 pages, and reported READY. Reassigned the public alias, then opened the plain demo URL in a fresh unauthenticated browser: HTTP 200, the updated profile contains its three remaining biography paragraphs and both source links, and the removed text is absent. The public exception survived the alias update.

Opened the plain public URL in a fresh browser context: HTTP 200, the BiGH homepage title and expected headline, no login redirect. The cellular-health images loaded and all 11 animations were present/running. The `/vn` route also returned HTTP 200 with `lang="vi"`. No page errors occurred. A final project-settings read confirmed general authentication remains enabled.

Configuration followed Vercel’s [domain exception documentation](https://vercel.com/docs/deployment-protection/methods-to-bypass-deployment-protection/deployment-protection-exceptions) and [alias CLI documentation](https://vercel.com/docs/cli/alias).
